ANTHROPIC_API_KEY=
PORT=3000

# Optional abuse-limit overrides (sensible defaults apply if left unset)
# MAX_MESSAGE_LENGTH=500
# PER_IP_WINDOW_MS=900000
# PER_IP_MAX_REQUESTS=15
# PER_CLIENT_DAILY_MAX=30
