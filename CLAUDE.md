real working chatbot (not a fake demo) on the website widget panel; WhatsApp and Instagram panels are interactive skins sharing the same chatbot backend and conversation as the website widget — just a different look (header/background/bubble styling) — with no real integration to the actual WhatsApp Business/Instagram Graph APIs; Node.js + Express server calling the Anthropic API; simple keyword-matching RAG to start; test entirely on localhost first, no hosting until it works locally; Render free tier for the cold-email demo link, upgrading to an isolated per-client instance only once someone pays. End it with your standing rule: always use Plan Mode, always write/update NOTES.md per stage, never start building without my explicit go-ahead.

